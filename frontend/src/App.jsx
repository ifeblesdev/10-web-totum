import React from 'react';
import GroupList from './GroupList'; // Importamos el componente que creamos

const App = () => {
    return (
        <div className="App">
            <GroupList /> {/* Llamamos al componente para que se muestre */}
        </div>
    );
};

export default App;
